version: 0.2

phases:
  install:
    commands:
      - echo Installing...

  pre_build:
    commands:
      - echo Security check

  build:
    commands:
      - echo Build phase

  post_build:
    commands:
      - echo Done